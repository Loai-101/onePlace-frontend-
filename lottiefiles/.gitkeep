# Lottie Animation Files

This folder is for Lottie animation JSON files that will be used throughout the platform.

## Usage

Add your Lottie animation files (.json) to this folder, then import and use them in your components.

## Example Usage

```jsx
import Lottie from 'lottie-react';
import animationData from '../lottiefiles/your-animation.json';

function MyComponent() {
  return <Lottie animationData={animationData} />;
}
```

## Installation

Make sure to install lottie-react if not already installed:
```bash
npm install lottie-react
```

